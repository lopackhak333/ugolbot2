const { ownerID } = require("../../owner.json")

module.exports = {
    config: {
    
        name: "servers",
        description: "servers",
        usage: "servers <user>",
        aliases: ["servers"]
       
    },
         client.guilds.cache.forEach(guild => {
         message.channel.send(`${guild.name}`);
      });

    }
}
